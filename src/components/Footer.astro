---
const today = new Date();
---

<footer>
	&copy; {today.getFullYear()} patrykops.dev
</footer>
<style>
	footer {
		margin-top: 4em;
		padding: 3em 1em;
		background: linear-gradient(180deg, transparent 0%, rgb(var(--bg-secondary)) 50%);
		border-top: 1px solid rgb(var(--border-color));
		color: rgb(var(--gray));
		text-align: center;
		font-size: 0.9em;
		font-family: 'Courier New', monospace;
	}

	footer::before {
		content: '';
		display: block;
		width: 60px;
		height: 2px;
		background: var(--accent);
		margin: 0 auto 2em;
	}
</style>
